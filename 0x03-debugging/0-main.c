#include "main.h"

/**
 * main - assigns a random number to int n everytime
 *
 * Checks ot see if a number is positive or negative and tests out with 0
 *
 * Return: Always 0 (Success)
 *
 **/

int main(void)

{
int i;

	i = 0;
	positive_or_negative(i);
return (0);

}
