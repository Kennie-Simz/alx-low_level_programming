#include <stdio.h>

/**
 * main - prints number of arguments passed into it
 *
 * @argc: gives number of commandline arguments
 * @argv:gives a list of commandline arguments involved
 *
 * Return: On success Always 0
 *
 **/

int main(int argc, char **argv)
{
	printf("%d\n, argc - 1);

	(void)argv;
	return (0);
}
